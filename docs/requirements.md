#### 下个版本完成



- 给`FoodEditScreen`表头中的`参选`增加一个表示”详情“的矢量图标，并且允许点击时（允许的点击区域应该包含`参选`和这个图标，最好是这一块的整个区域）弹出一个对话框（解释“参选”到底是干嘛，并且可以点击“确定”或者对话框外隐去）     **v0.8完成**

- 将`FoodEditScreen`的单表格改为多表格，并且在`EatScreen`屏幕右侧加上一列`书签`（即快捷表格选择），`书签`上应包含`表名`，常规状态下可能显示不全，但长按时或者点击时应该向左滑出，显示完整`表名`。同时将`FoodEditScreen` 的表格右侧也插上书签，这里可以简略点              **v0.8完成**

  

#### 后续计划完成

- 增加后端数据库，用于存放`菜`，每个`菜`包含`菜名`、`菜品预览图列表`、`详情`、`用户评价`、`平均评分`，菜名需要保证唯一
- 增加用户上传评价的方式，用户可以输入菜名进行选择，或者新建一个`菜`（要求至少上传`菜名`和`评分`，其他可选）
- 增加展示大众上传与评价的菜品的页面，主要按`评分`降序排序并显示，长按每个`菜`可以查看更多`菜品预览图`和`用户评价`
- 增加日历功能
- 增加待办事项功能